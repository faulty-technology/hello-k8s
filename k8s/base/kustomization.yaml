apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# APP-SPECIFIC: Change this to your app name - propagates to all resources
labels:
  - pairs:
      app: hello-k8s
    includeSelectors: true

resources:
  - deployment.yaml
  - service.yaml
